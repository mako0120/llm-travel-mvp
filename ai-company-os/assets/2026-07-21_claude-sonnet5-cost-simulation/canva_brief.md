# Canva ブリーフ: Claude Sonnet 5 運用費試算デッキ

<!-- templates/canva_brief_template.md 準拠。Canva 未接続のため人間が移植する前提の仕様書 -->

## メタ情報

- デッキ名: Claude Sonnet 5、"エージェント運用費"は結局いくら?
- 元になる仕様: `assets/2026-07-21_claude-sonnet5-cost-simulation/deck_spec.json`(全30スライドのテキストはこのJSONが正)
- 目的・想定視聴者: 18〜45歳、AI初心者・個人事業主・小規模事業者向け。目的は「導入価格の締切とトークナイザー変更の実質コストを理解し、8月中に自分で試算してもらう」こと
- パレット(HEX): Primary `#2B3A55` / Base `#D3E4F5` / Accent `#1F8FD6`(build_deck.pyの`slate_azure`プリセットと同一)
- フォント指定(Canva代替): Yu Gothic → Noto Sans JP
- アニメーション: なし(情報密度が高いテーマのため、装飾より可読性を優先)
- 使用素材のライセンス確認: 無料素材のみを想定。Canva Pro素材を使う場合は導入前に人間承認が必要

## レイアウト種別 → Canva要素の対応表

deck_spec.json のレイアウト名は、Canvaでは以下の要素種別に読み替える。

| deck_spec.jsonのlayout | Canva要素 | 該当スライド |
|---|---|---|
| title / cta | タイトルレイアウト(濃色背景+大見出し) | 1, 30(cta) |
| bullets | 見出し+ブレットリスト | 3,4,6,7,9,13,14,15,20,25,26,27,28 |
| cards | カードグリッド(2〜3枚) | 2,5,12,21,22 |
| agenda | 濃色背景+番号付きリスト | 8 |
| table | 表要素 | 10,17,18,24,29 |
| comparison | 2カラムレイアウト | 11,19 |
| bar_chart | 棒グラフ要素 | 18 |

(スライド番号は `deck_spec.json` の `slides` 配列の出現順。テキスト本文・出典・ノートは同JSONを参照してそのままコピーする)

## スライドごとの指定(代表例)

30枚全ての本文をここに複製するとJSONと二重管理になるため、Canvaでの表現が
PowerPoint版と異なる箇所・特に注意が必要な箇所のみ記載する。それ以外は
deck_spec.json の該当スライドのテキストをそのままCanvaへ転記する。

### スライド 1(title)

- レイアウト種別: タイトル
- 図版指示: 背景は`primary_dark`の単色。中央に見出し、下部にサブコピーと出典を小さく配置
- 配色指定: 背景`#1B2537`相当、見出し文字は白、サブコピーは`#D3E4F5`
- 備考: Canvaのプレゼンテーション用テンプレート「Simple Dark」系が近い

### スライド 18(bar_chart)

- レイアウト種別: チャート(棒グラフ、3本)
- 図版指示: Canvaのグラフ要素で「Sonnet5導入価格/Sonnet5標準価格/Opus4.8」の3本棒グラフを作成。データは$36/$54/$130
- 配色指定: 3本とも`#1F8FD6`(Accent)で統一し、装飾色を混在させない
- 備考: PowerPoint版はpython-pptxのネイティブチャート(編集可能)。Canva版は手動でグラフ要素にデータ入力する

### スライド 10, 17(table)

- レイアウト種別: 表
- 図版指示: ヘッダー行を`primary`色で塗りつぶし、白文字。データ行は白/`card`色を交互に
- 備考: 数値は仮定条件に基づく試算である旨のフッター注記を必ず残す(誇張防止)

## 移植手順(人間向け)

1. Canvaで16:9プレゼンテーションを新規作成し、上記パレットをブランドカラーに登録
2. `deck_spec.json` を開き、スライドごとに `title`/`heading`/`bullets`/`cards`等のテキストを、上記の対応表に従ってCanva要素にコピー
3. 表・グラフのスライドは上記「代表例」の指示に従って手動で再現
4. 完成後、PowerPoint版(`build_deck.py`で生成したもの)と並べて全30枚の数字・出典が一致しているか確認

## 検品チェック

- [ ] 全30スライドの指定(テキスト転記)が完了している
- [ ] Pro素材の使用有無を明記した(現時点: 無料素材のみを想定、未使用)
- [ ] PowerPoint版と数字・出典が一致している(deck_spec.jsonが両者の単一の情報源)
